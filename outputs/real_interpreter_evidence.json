{
  "_purpose": "Proof that the B3 bounded interpreter was actually run against a live model at least once, not only exercised through the deterministic_offline stub used everywhere else in this repository's tests and benchmarks.",
  "_reproduce": "export OPENAI_API_KEY=<your key>; export MANDATEGUARD_INTERPRETER_BASE_URL=https://api.groq.com/openai/v1; export MANDATEGUARD_INTERPRETER_MODEL=openai/gpt-oss-20b; python3 -m bailiff.demo --real-interpreter",
  "provider": "Groq (OpenAI-compatible endpoint, free tier)",
  "provider_base_url": "https://api.groq.com/openai/v1",
  "fixture_case": "R3_AMBIGUOUS bounded-interpreter case, generated by bailiff.demo.make_ambiguous_event",
  "run_utc": "2026-08-25",
  "result": {
    "reason": "UNKNOWN_OR_CONFLICTING",
    "confidence": 0.9,
    "model": "openai/gpt-oss-20b",
    "model_calls": 1,
    "model_tokens": 651,
    "model_cost_inr": 0.011007,
    "reason_source": "MODEL_INTERPRETATION"
  },
  "notes": [
    "The interpreter is an annotation service only: it returns one taxonomy reason and a confidence score. It cannot authorize a retry, change consent, or call the provider — that boundary is enforced in bailiff/guardrails.py, not by the model's good behaviour.",
    "In this run the model's own confidence (0.9) still did not cross the threshold the guardrail engine requires to act on a model-sourced reason for this case shape, so the case still escalated to HUMAN_REVIEW_REQUIRED with zero provider calls — the same outcome as the deterministic_offline stub. That is the point being demonstrated: the model is consulted, not trusted, and a wrong or low-confidence answer cannot widen what the runtime is willing to do.",
    "model_cost_inr uses Groq's published list price for openai/gpt-oss-20b ($0.10/1M input, $0.50/1M output tokens as of August 2026), set via MANDATEGUARD_MODEL_INPUT_USD_PER_1M / MANDATEGUARD_MODEL_OUTPUT_USD_PER_1M, converted at the repository default of 85 INR/USD.",
    "No API key is stored in this repository. Reproducing this run requires the operator's own key in OPENAI_API_KEY at run time."
  ]
}
